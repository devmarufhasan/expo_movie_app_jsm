import { Text, View } from "react-native";

export default function Index() {
    return (
        <View>
            <Text className="text-[70px] font-bold text-primary">
                Welcome !
            </Text>
        </View>
    );
}
